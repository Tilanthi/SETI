# arXiv upload set for v3.45

Verified 2026-09-11 (referee cycle 3) by running `./arxivset.sh` into an empty
directory containing only the files below, then `pdflatex` x3: **29 pages,
0 errors, 0 undefined references/citations, 0 multiply-defined labels, 0
overfull boxes, 0 underfull boxes, 0 Type 3 fonts, 0 missing files**, 1.5 MB.
Nothing else in this folder is needed by arXiv; the `.py` generators, the
`.json`/`.csv` data products, the referee letters and BUILD_NOTES are
provenance rather than build inputs.

**27 items: 20 tex/cls/sty + 7 figures.**

- `technosignatures_20pc_v3.45.tex`   (bibliography is inline; no .bbl/.bst needed)
- `openjournal.cls`                   (Open Journal of Astrophysics class)
- `epsf.sty`
- `survey_numbers.tex`
- `survey_numbers_round5.tex` ... `survey_numbers_round14.tex` (ten files)
- `survey_numbers_recurrence.tex`  (CP-72 2713 two-epoch test; `\input` at line 30)
- `survey_numbers_localnull.tex`
- `tab_compcurve.tex`, `tab_occurrence.tex`, `tab_selection.tex`, `tab_perband.tex`
- `figures/` (7 PDFs, all of them used):
  `completeness`, `control_diagnostics`, `coverage_waterfall`,
  `cp72_control_distribution`, `drift_acceleration`, `eirp_context`,
  `sensitivity_2d`

The list is generated, never maintained by hand: it is exactly `\input{}` and
`\includegraphics{}` resolved out of the manuscript, plus the class and style
file. Regenerate and re-verify with `./arxivset.sh`.

## Changes from v3.44's upload set

- **On the three referees' manifest complaint (1 C3, 2 N10, 3 N5): partly
  confirmed, partly not.** All three report that this file said "26 items" and
  omitted `survey_numbers_recurrence.tex` from its list. The copy in this
  folder says "Total 1.9 MB, 27 items" and does list
  `survey_numbers_recurrence.tex`, so those two specifics do not reproduce
  here and are recorded as not confirmed. What was stale and is fixed: the
  title still said v3.44; the size 1.9 MB was 1.5 MB measured; and the final
  bullet of the v3.43 section still read "the item count falls 27 -> 26",
  contradicting the header on the same page. `arxivset.sh` was correct
  throughout and resolves 27 items.
- **The item count and the file set are unchanged at 27** between v3.44 and
  v3.45. `tab_ring.tex` and `figures/noise_qa.pdf` remain generated-but-unused
  data products, and `tab_smear.tex` likewise.
- `figures/cp72_control_distribution.pdf` regenerated: it now carries **both
  epochs** of the CP-72 2713 Band 7 window, the two control ensembles, both
  control maxima and both values of `T_star`, at the same canvas size and so
  at no cost in page (referee 3, N8).
- `make_all.sh` now runs `recurrence_calc.py`, without which a clean
  regeneration leaves `survey_numbers_recurrence.tex` absent and the paper
  does not build (referee 3, N5). It also restores
  `survey_numbers_round{5,6,7}.tex` from `frozen_macros/` first: those three
  have no generator in the release, which the same clean-regeneration test
  found independently.

## Changes from v3.43's upload set

- **`survey_numbers_round14.tex` is new** (90 macros, generated by
  `v344_calc.py`). It carries the archive epoch audit that the previous round
  computed and did not print, the CP$-$72 2713 second-block disclosure, the
  corrected baseline-node channel, the belt geometry and CO limits, the
  control-ensemble calibration of the 5-sigma threshold, and the ACA
  consequences.
- **`tab_ring.tex` is no longer used**: Table 3 (control-annulus geometry by
  band) was deleted on referee 1's length plan, the per-window geometry being
  six columns of the released catalogue. The fragment is still generated and
  released as a data product.
- **`figures/noise_qa.pdf` is no longer used**: Fig. 3 was deleted on the same
  plan, referee 1 having verified both of the numbers it plots without it.
  It ships with the data.
- `figures/coverage_waterfall.pdf` regenerated: the legend no longer emits a
  literal `\,`, alternate row labels are set on a right-hand axis so the 81
  system names no longer collide at 0.72\textwidth, and Greek and possessive
  designations are set as the text sets them.
- `figures/eirp_context.pdf` regenerated: the in-figure annotation no longer
  contains a spaced double hyphen.
- **`make_all.sh` gained a last step**, `retire_macros.py`, which strips from
  the generated macro files every `\newcommand` the manuscript does not
  reference. The upload set is unaffected in content; the macro files are
  smaller and the unused-macro count is zero.
- The item count is 27: `survey_numbers_round14.tex` and
  `survey_numbers_recurrence.tex` in, `tab_ring.tex` out, and the figure set
  8 -> 7. (This bullet read "falls 27 -> 26" until v3.45 and was wrong.)

- **Every shipped figure now has a generator in this folder** (referee 3,
  minor 5). `make_fig_missing_v344.py` rebuilds
  `figures/cp72_control_distribution.pdf` from
  `localnull_code/ctrlmax.json` and the catalogue, and
  `figures/sensitivity_2d.pdf` from the catalogue alone, at the shipped canvas
  sizes. Both reproduce the figures they replace: the histogram's 32 bars and
  its two marked values, and all 431 points with the same class split,
  markers, colours and labels. The condition that retired
  `pipeline_schematic.pdf` in v3.42, a figure with no script behind it, no
  longer applies to any figure here.

## Changes from v3.42's upload set

- **`survey_numbers_round13.tex` is new** (129 macros, generated by
  `v343_calc.py`). It carries every number the three v3.43 referee reports
  showed to be hand-typed, stale or contradicted by the shipped catalogue: the
  nine-window smearing census, the primary-beam distribution and the
  j1256$-$1257 maximum, the beta Pic empirical significances on the symmetric
  statistic, the Band 4--8 union, the gross/union ratio, the baseline width W,
  the trials budget in cells, the crossing edge-proximity distribution, the
  array and Hanning state harvested from the archive, and the CP$-$72 2713
  line-catalogue query at the corrected crossing frequency.
- `\EffBandGHz` was retired from `make_numbers.py` and `round9_calc.py`; its
  three uses take `\SearchedUnionGHz`, which is now the single source.
- `figures/coverage_waterfall.pdf` regenerated at larger label fonts, because
  the manuscript now includes it at 0.80 rather than 0.90 `\textwidth`.
- The item count rises 26 -> 27 for `survey_numbers_round13.tex`; the figure
  set is unchanged at 8.

## Changes from v3.41's upload set

- `figures/pipeline_schematic.pdf` is **no longer used**. It was the sole
  source of the 3 Type 3 fonts inherited since v3.39, it duplicated
  Table~2 (the statistical data path), and it carried ~12 hand-typed
  survey numbers outside the paper's single-source rule. The manuscript now
  reports **0 Type 3 fonts**.
- `figures/bpic_velocity_space.pdf` is **no longer used** (phase 3). Every
  number it plotted is in Table~8 (the frame-chain audit) or Table~7
  (CP$-$72 2713's line offset), it was not asked for by either referee, and it
  was the last figure in the set with no generator in the release folder. See
  BUILD_NOTES phase 3.
- `figures/completeness_surface.pdf` went with the appendix triage: the upload
  set is down from 24 figure PDFs in v3.41 to the 8 the manuscript includes.
- `survey_numbers_round12.tex` (control-ring geometry, repaired line mask,
  Hanning correction, per-class sensitivity), `tab_ring.tex`,
  `tab_selection.tex` and `tab_perband.tex` are new build inputs.
- `survey_numbers_localnull.tex` carries the **measured** second-stage local
  null (42 `\LN*` macros generated by `localnull_calc.py` from
  `v342_local_null.json`). In phase 1 it held a single placeholder macro
  `\LocalNullCP`; that macro no longer exists anywhere in the manuscript.
