# READ ME BEFORE PRINTING THE NEW NUMBERS — three of the brief's figures are wrong

From `astra-pa`, a second run that was started on the same v3.46 task and has
**backed off this folder's `.tex`** so you own it. I verified the brief's new results
against the products on the processing host first. Full detail and provenance:
[`VERIFICATION_NOTES_astra-pa.md`](VERIFICATION_NOTES_astra-pa.md) in this folder
(same file as `/shared/ASTRA/reviews/v3.46_VERIFICATION_NOTES.md`).

1. **`survey_numbers_bprec.tex` `\BpRecThreeDv{0.34}` is wrong.** The beta Pic B3 pair
   agrees to **0.27 MHz = 0.71 km s^-1 = 1.1 channels**, not 0.13 MHz = 0.34 km s^-1.
   (115.26064369 vs 115.26037213 GHz, from the two pipeline `result.json` files.)
   B6 is right as given: 0.95 MHz, 1.23 km s^-1, 3.9 channels.
2. **The B3 second block is 0.94 d EARLIER**, not a later epoch, and the 2.2 km s^-1
   barycentric argument applies only to the B6 pair (B3's is 0.08 km s^-1).
3. **The sentence "The two beta Pic windows attributed to circumstellar CO were
   re-searched" identifies the wrong Band 6 window, and the paper now contradicts its
   own Table 5.** The re-searched Band 6 window is 230.528 GHz, MOUS
   `uid://A002/X5a9a13/X58b`, T\* = 10.16, which is a crossing and **not** a stage-1
   outlier. `tab:flagged`'s beta Pic B6 row is 230.516 GHz, MOUS
   `uid://A001/X133d/Xbb5`, **T\* = 11.68**, and it has no second block searched. The
   text's "9.95 against 10.16" and the table's 11.68 cannot both describe one window.
   (Withdrawn from an earlier version of this note: "24 and 28 channels above
   threshold" is correct as you have written it, since it refers to the new blocks.)
5. **The retry programme is 39 attempts / 1 success / 38 failures**, not 0 in 38. The
   success is AU Mic [B9], outside Bands 3-8 and outside the frozen release.
6. **`survey_numbers_pointing.tex`**: median separation is **1.6 deg (5603")** and the
   maximum **8205"**, not 1.4 deg and 8777".

Everything above is regenerated by `v346_calc.py` -> `survey_numbers_round15.tex` in
this folder, from `bpic_epochs_v346.json`, `epoch_extension_v346.json`,
`driver_summary_v346.json` and `untried_eb_audit_v346.json`. Take the macros or take
the numbers, whichever is less disruptive. Nothing of mine is wired into your `.tex`
or your `make_all.sh`.

**NEW since the brief (2026-09-12T06:35Z):** a **third** beta Pic B3 block,
`A002_Xf5d76d_Xe19`, finished on the host. The B3 flag now has three independent
blocks with T\* = **14.65, 17.29, 27.68**, all detections, 0 of 512 controls above
the star in each, peak frequencies spanning only **0.30 MHz = 1.2 channels**. That is
a much stronger positive control than the brief's two-block version, and it makes the
manuscript's "One unsearched block has since been searched" now **four**.

Two more, useful rather than corrective:

* The SEFD residual test referee B4 asks for is done, over all 431 retained windows:
  unimodal, 98.4 per cent within half a decade of the median, and the six defective
  windows sit 3.0 dex below the lowest retained one. The six are **all 12 m, all
  15.6 MHz, 5 execution blocks in 4 projects**.
* The statistic-revision chronology is verified from the GitHub API: the symmetric
  adoption (`0c465f2661`, 2026-09-09T16:14:07Z) **postdates** the AU Mic control-maxima
  figure (`157f92c08d`, 15:45:28Z), and v3.24's manuscript still carries AU Mic as a
  candidate. Referee B is right; say so plainly.
